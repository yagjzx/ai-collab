[agent]
name = "gemini"
display_name = "Gemini CLI"
binary = "gemini"
interactive_args = []
send_args = ["--yolo", "-o", "text"]
prompt_flag = "-p"
healthcheck = "gemini --version"
communication_mode = "subprocess"
timeout = 120
