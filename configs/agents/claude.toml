[agent]
name = "claude"
display_name = "Claude Code"
binary = "claude"
interactive_args = []
send_args = []
prompt_flag = "-p"
healthcheck = "claude --version"
communication_mode = "subprocess"
timeout = 120
